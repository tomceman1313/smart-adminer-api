model Notification {
    id           Int       @id @default(autoincrement())
    title        String
    description  String
    urlPath      String
    type         String
    fromDateTime DateTime?
    toDateTime   DateTime?
}
